# Bounded on both sides so a future major release cannot silently move the
# published estimates. The versions in the trailing comments are the ones the
# README's numbers and the committed figures were produced with (Python
# 3.13.7); `pyproject.toml` carries the same bounds as package metadata.
pandas>=2.2,<3            # 2.3.2
numpy>=1.26,<3            # 2.3.3
requests>=2.31,<3         # 2.32.5
python-dotenv>=1.0,<2     # 1.2.2
statsmodels>=0.14,<0.16   # 0.14.6
linearmodels>=6.0,<8      # 7.0
pyarrow>=15.0,<23         # 21.0.0
openpyxl>=3.1,<4          # 3.1.5
scipy>=1.11,<2            # 1.16.3
matplotlib>=3.8,<4        # 3.10.6
plotly>=5.20,<7           # 6.5.0
streamlit>=1.35,<2        # 1.51.0

# Development
pytest>=8.0,<9            # 8.4.2
ruff>=0.6,<0.15
